# ?????
server.port=8080
server.servlet.context-path=/api

# ?????
spring.datasource.url=jdbc:mysql://localhost:3306/driving_school?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT ??
spring.security.jwt.secret=${JWT_SECRET}
spring.security.jwt.expiration=3600

# ????
logging.level.root=INFO
logging.level.com.driving=DEBUG
logging.level.org.springframework.security=OFF

# CORS ??
spring.mvc.cors.allowed-origins=https://your-frontend-domain.com
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=Authorization,Content-Type,X-Requested-With

# ??????
spring.datasource.initialization-mode=always
spring.datasource.schema-locations=classpath:data.sql

# ???????
spring.devtools.restart.enabled=true